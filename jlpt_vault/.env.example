# Copy to `.env` in this directory (`jlpt_vault/`) for `docker compose`.
# Defaults match docker-compose substitution; override for anything non-local.

POSTGRES_USER=dev_user
POSTGRES_PASSWORD=dev_password
POSTGRES_DB=jlpt_vault
